apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: controller-clusterrole-installed
spec:
  description: Asserts the main controller ClusterRole exists.
  steps:
  - try:
    - assert:
        resource:
          apiVersion: rbac.authorization.k8s.io/v1
          kind: ClusterRole
          metadata:
            name: magos
