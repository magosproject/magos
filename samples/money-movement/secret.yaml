# In production this Secret is provisioned by an external secret manager
# (External Secrets Operator, Vault Agent, sealed-secrets) and rotated out of
# band. The placeholder below exists only so the sample applies cleanly in a
# local kind cluster; it MUST NOT be checked in to a real cluster.
apiVersion: v1
kind: Secret
metadata:
  name: money-movement-ledger-admin
  namespace: money-movement
  labels:
    app.kubernetes.io/part-of: money-movement-platform
    app.kubernetes.io/managed-by: magos
type: Opaque
stringData:
  password: placeholder-replace-via-eso
