apiVersion: policies.kyverno.io/v1
kind: ValidatingPolicy
metadata:
  name: require-token-entropy
  labels:
    policy.magosproject.io/category: compliance
spec:
  evaluation:
    mode: JSON
  matchConditions:
    - name: is-terraform-plan
      expression: 'has(object.planned_values) && has(object.terraform_version)'
  validations:
    - message: 'Require random_id.server byte_length to be 1'
      expression: |
        (
          (has(object.planned_values.root_module.resources) &&
          object.planned_values.root_module.resources.exists(r, r.type == 'random_id' && r.name == 'server' && r.values.byte_length == 1))
        )
