apiVersion: magosproject.io/v1alpha1
kind: Workspace
metadata:
  name: dns-staging
  namespace: default
  labels:
    env: staging
  annotations:
    magosproject.io/reconcile-interval: "120s"
spec:
  projectRef:
    name: networking-with-policies
  serviceAccountName: magos-job
  validation:
    policySelector:
      matchLabels:
        policy.magosproject.io/category: network-security
  source:
    repoURL: https://github.com/magosproject/demo.git
    targetRevision: main
    path: .
  terraform:
    version: "1.14.8"
    tfvarsPath: env/staging/vars.tfvars
