{{- include "magos.validate" . -}}
{{- if eq (include "magos.postgresMode" .) "embedded" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "magos.postgresHeadlessName" . }}
  labels:
    {{- include "magos.labels" . | nindent 4 }}
    {{- include "magos.postgres.labels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    {{- include "magos.selectorLabels" . | nindent 4 }}
    {{- include "magos.postgres.labels" . | nindent 4 }}
  ports:
  - name: postgres
    port: {{ .Values.postgres.embedded.service.port }}
    targetPort: postgres
    protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "magos.postgresName" . }}
  labels:
    {{- include "magos.labels" . | nindent 4 }}
    {{- include "magos.postgres.labels" . | nindent 4 }}
spec:
  type: {{ .Values.postgres.embedded.service.type }}
  selector:
    {{- include "magos.selectorLabels" . | nindent 4 }}
    {{- include "magos.postgres.labels" . | nindent 4 }}
  ports:
  - name: postgres
    port: {{ .Values.postgres.embedded.service.port }}
    targetPort: postgres
    protocol: TCP
    {{- if and (eq .Values.postgres.embedded.service.type "NodePort") .Values.postgres.embedded.service.nodePort }}
    nodePort: {{ .Values.postgres.embedded.service.nodePort }}
    {{- end }}
{{- end }}
